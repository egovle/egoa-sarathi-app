
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // This rule allows anyone to read and write any file.
    // WARNING: This is insecure and for development ONLY.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

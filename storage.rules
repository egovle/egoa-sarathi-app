rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Require a valid App Check token for all reads and writes
    match /{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.app_check == true;
    }
  }
}

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access to any file for any authenticated user.
      // This is a relaxed rule to ensure functionality.
      allow read, write: if request.auth != null;
    }
  }
}

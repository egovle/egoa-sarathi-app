rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow write access for any authenticated user to any path.
    // This is a temporary, permissive rule to unblock task creation.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

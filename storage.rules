rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Require a valid App Check token for all storage access.
    // This is a basic rule; you can add more specific user-based authentication later.
    match /{allPaths=**} {
      allow read, write: if request.resource.app.token != null;
    }
  }
}
